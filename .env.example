# Public client config (safe to expose; baked into the bundle).
# The authoritative server runs as a Supabase Edge Function; its privileged key is
# platform-injected there and never lives in this repo, the client, or Vercel.
VITE_SUPABASE_URL=https://fnandjtzwhihgefkfwzj.supabase.co
VITE_SUPABASE_ANON_KEY=<anon key — public by design>
